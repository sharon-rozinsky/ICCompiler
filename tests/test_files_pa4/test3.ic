class B {
	int y;

	static void func(int y){
		Library.printi(y);
	}
}

class A extends B{
  int x;
  string s;
  
  static void foo(int y) {
    int z=y+1;

    func(z);
    Library.printi(5);
  }

  static void main(string[] args) {
    int p = 5;
    A a = new A();
    B b = new B();
 	
 	A.func(4);
 	A.foo(p);
 	Library.println("");
 	//func(2);
  }
}